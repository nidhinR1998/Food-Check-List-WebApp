package com.springbootfproject.firstWebApp.service;

import org.springframework.stereotype.Component;

@Component
public class loginAuthenticationService {
	
	/* USES FOR TESTING
	 * public boolean authenticate(String username, String password) { boolean
	 * isValidUsername =username.equalsIgnoreCase("nidhin"); boolean isValidPassword
	 * = password.equalsIgnoreCase("nidhin123"); return isValidUsername &&
	 * isValidPassword; }
	 */

}
